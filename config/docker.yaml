---
my-docker:  # Referenced by 'server: my-docker' in services.yaml
  host: dockersocket  # Using docker-socket-proxy for security instead of exposing socket directly
  port: 2375

# Direct socket access (less secure - grants full Docker API access):
# my-docker:
#   socket: /var/run/docker.sock
